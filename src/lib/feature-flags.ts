/**
 * Toggle to show outline/summary in human-facing UI (Share page HTML, recorder OutlinePanel).
 * When false, artifacts are hidden from these UIs but still included in Share markdown and JSON for agents.
 * Set to true to bring the structure panel and share-page summary/outline back.
 */
export const SHOW_ARTIFACTS_IN_UI = false;
